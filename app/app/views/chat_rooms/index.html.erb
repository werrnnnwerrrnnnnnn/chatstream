<% content_for :title, "Chat Rooms" %>

<div class="container">
  <div class="d-flex justify-content-between align-items-center mb-4">
    <h1 class="mb-0">💬 Chat Rooms</h1>
    <%= link_to "➕ New Chat Room", new_chat_room_path, class: "btn btn-success" %>
  </div>

  <div id="chat_rooms" class="row g-4">
    <% @chat_rooms.each do |chat_room| %>
      <% if chat_room.present? %>
        <div class="col-md-6">
          <div class="card h-100 shadow-sm">
            <div class="card-body">
              <p class="fw-bold mb-1">
                <span class="d-inline-block text-truncate" style="max-width: 100%;" title="<%= chat_room.name %>">
                  <%= truncate(chat_room.name, length: 70) %>
                </span>
              </p>

              <%= link_to "🔗 View Room", chat_room_path(chat_room), class: "btn btn-outline-primary mt-3" %>
            </div>
          </div>
        </div>
      <% end %>
    <% end %>
  </div>
</div>