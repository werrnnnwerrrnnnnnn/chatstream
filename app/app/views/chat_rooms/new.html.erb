<% content_for :title, "New Chat Room" %>

<div class="container mt-5">
  <div class="card shadow-sm">
    <div class="card-body">
      <h1 class="card-title mb-4">➕ New Chat Room</h1>

      <%= render "form", chat_room: @chat_room %>

      <div class="mt-4">
        <%= link_to "⬅️ Back to Chat Rooms", chat_rooms_path, class: "btn btn-outline-secondary" %>
      </div>
    </div>
  </div>
</div>